# FastAPI — the web framework for building the API
fastapi==0.104.1

# Uvicorn — the ASGI server that runs FastAPI
# (FastAPI is an async framework, it needs an async server)
uvicorn==0.24.0

# HuggingFace Transformers — loads tokenizer
transformers==4.35.2

# ONNX Runtime and Optimum — for fast CPU inference
onnxruntime>=1.15.0
optimum[onnxruntime]>=1.14.0

# Pydantic — data validation for request/response models
pydantic==2.5.2

# NumPy — numerical operations (used for softmax on predictions)
numpy==1.26.2
